import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Toaster } from '@/components/ui/toaster';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'VyaparDesk — Inventory & Billing Management',
  description: 'Complete inventory and billing management system for local businesses. Manage products, customers, suppliers, purchases, and generate invoices.',
  keywords: 'inventory management, billing, POS, retail, shop management, vyapardesk',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
